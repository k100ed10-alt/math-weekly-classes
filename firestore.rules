rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /settings/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /weeklyClasses/{id} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
